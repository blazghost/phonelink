
-- phonelink:messages -- the Messages window shows photos and video, and
-- Omarchy's default translucency lets the desktop show through them.
o.window("^(org\\.omarchy\\.phonelink\\.messages)$", { tag = "-default-opacity", opacity = "1 1" })
